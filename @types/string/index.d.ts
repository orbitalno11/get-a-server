interface String {
    isSafeNotNull(): boolean

    isSafeNotBlank(): boolean

    isBlank(): boolean

    isNotBlank(): boolean

    toNumber(): number

    isBoolean(): boolean

    isNumber(): boolean
}
